Within a python notebook, develop snippets to calculate scorecard for rating the EU countries. The scoring is represented by a number between 1 and 100 . Start by taking to attributes: the nominal GDP and the inflation rate for the year 2025. The for each attribute, compute the score, the combine them using weights as for w1 and w2; two sets of weights may applying dipending on whether the country is classified as Y or N. This masking should be given in a separate data structure. Determine the appropriate data structure (dataframes, numpy arrays,...) so to use matrix arithmetics. This may entail building a matrix for the weaights (perhaps with 0 and 1 for applying the mask). USe different cells for different snippets.